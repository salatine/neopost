title: "cat blog!!"
baseURL: 'https://neopost.neocities.org'
languageCode: 'en-us'
titleCaseStyle: 'none' # comment this line for forcing capitalized titles on tags
theme: "neopost"

languages:
  en:
    languageCode: en-us
    contentDir: content/en
    languageName: english
    weight: 1
    params:
      sidebar:
        tags-limit: 1 # limit showcasing only up to this number of tags in bio, defaults to 5
        more-tags-text: "more tags..." # show a hyperlink with this text if there's more tags than tags-limit

      read-more:
        length-limit: 120 # if the text exceeds this number of characters, the read more button will appear to expand the text
        text:
          more: "read more"
          less: "read less"
  jp:
    languageCode: ja-jp
    contentDir: content/jp
    languageName: 日本語
    weight: 2
    params:
      sidebar:
        tags-limit: 1 # limit showcasing only up to this number of tags in bio, defaults to 5
        more-tags-text: "その他のタグ。。。" # show a hyperlink with this text if there's more tags than tags-limit

      read-more:
        length-limit: 80 # if the text exceeds this number of characters, the read more button will appear to expand the text
        text:
          more: "もっと読む"
          less: "もっと読む"

permalinks:
  posts: "/posts/:filename/"

params:
  theme: "light-blue" # light-blue, dark-blue, light-green, light-yellow, light-pink, light-purple
  favicon: "/favicon.ico"
  posts-per-page: 1 # defaults to 5 if not defined
  toc-auto-numbering: false
